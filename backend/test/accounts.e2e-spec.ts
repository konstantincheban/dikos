describe('AccountsController (e2e)', () => {
  it('/ (GET) - get all accounts by userID', () => {
    return Promise.resolve(true);
  });
  it('/summaryData/:id (GET) - get account summary data', () => {
    return Promise.resolve(true);
  });
  it('/create (POST) - create account', () => {
    return Promise.resolve(true);
  });
  it('/edit/:id (POST) - edit account', () => {
    return Promise.resolve(true);
  });
  it('/delete/:id (POST) - delete account', () => {
    return Promise.resolve(true);
  });
});
