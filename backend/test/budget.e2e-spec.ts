describe('BudgetController (e2e)', () => {
  it('/:id (GET) - get budget data', () => {
    return Promise.resolve(true);
  });
  it('/create (POST) - create new budget entry', () => {
    return Promise.resolve(true);
  });
  it('/edit/:id (POST) - edit budget data', () => {
    return Promise.resolve(true);
  });
});
