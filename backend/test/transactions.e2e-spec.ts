describe('TransactionsController (e2e)', () => {
  it('/ (GET) - get all transactions by userID', () => {
    return Promise.resolve(true);
  });
  it('/create (POST) - create transaction', () => {
    return Promise.resolve(true);
  });
  it('/edit/:id (POST) - edit transaction', () => {
    return Promise.resolve(true);
  });
  it('/delete/:id (POST) - delete transaction', () => {
    return Promise.resolve(true);
  });
});
